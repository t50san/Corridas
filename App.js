import { ScrollView, View } from 'react-native';
import { Container } from './app/src/styles/custom';

export default function App() {
  return (
    <ScrollView contentContainerStyle={{ flexGrow: 1}}>
      <Container>
      </Container>
    </ScrollView>
    
  );
}